# Generated by theme-switcher ({{NAME}}) — sourced by hyprlock.conf

$lock_bg_color = {{LOCK_BG}}
$lock_fg = {{LOCK_FG}}
$lock_accent = {{LOCK_ACCENT}}
$lock_accent_base = {{LOCK_ACCENT_BASE}}
$lock_inner = {{LOCK_INNER}}
