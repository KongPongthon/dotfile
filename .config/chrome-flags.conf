# Google Chrome flags for Hyprland / Wayland
--ozone-platform-hint=auto
--enable-features=UseOzonePlatform,WaylandWindowDecorations
# Use the profile password DB. Login already unlocked the session; do not create a new keyring.
--password-store=basic
