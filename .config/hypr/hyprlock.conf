# Session lock — wallpaper + centered user / password
# lock.bg is a symlink updated by set-wallpaper.sh / install.sh
# Colors are generated by theme-switcher into hyprlock-colors.conf

source = ~/.config/hypr/hyprlock-colors.conf

general {
    hide_cursor = true
    grace = 0
    ignore_empty_input = false
}

animations {
    enabled = true
    bezier = easeOut, 0.16, 1, 0.3, 1
    animation = fadeIn, 1, 4, easeOut
    animation = fadeOut, 1, 3, easeOut
    animation = inputFieldDots, 1, 2, easeOut
}

background {
    monitor =
    path = ~/.config/hypr/lock.bg
    color = $lock_bg_color
    blur_passes = 2
    blur_size = 4
    brightness = 0.75
    contrast = 1.05
}

label {
    monitor =
    text = $USER
    color = $lock_fg
    font_size = 28
    font_family = JetBrainsMono Nerd Font
    position = 0, 48
    halign = center
    valign = center
}

label {
    monitor =
    text = $TIME
    color = $lock_accent
    font_size = 56
    font_family = JetBrainsMono Nerd Font
    position = 0, 140
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 320, 52
    outline_thickness = 2
    dots_size = 0.25
    dots_spacing = 0.25
    dots_center = true
    rounding = 10
    outer_color = $lock_accent_base
    inner_color = $lock_inner
    font_color = $lock_fg
    fade_on_empty = false
    placeholder_text = <i>Password</i>
    fail_text = <i>$PAMFAIL</i>
    position = 0, -40
    halign = center
    valign = center
}
