# Session / toolkit
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = GDK_BACKEND,wayland,x11,*
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = MOZ_ENABLE_WAYLAND,1
env = ELECTRON_OZONE_PLATFORM_HINT,auto

# Cursor / apps
env = XCURSOR_SIZE,24

# Hybrid Intel + NVIDIA (MSI GF63):
# Do NOT force LIBVA / __GLX_VENDOR_LIBRARY_NAME=nvidia globally —
# that often breaks Chrome GPU / VAAPI. Leave defaults (Intel) for UI apps.
# Uncomment only if you intentionally want NVIDIA GLX for a specific need:
# env = __GLX_VENDOR_LIBRARY_NAME,nvidia
# env = NVD_BACKEND,direct
